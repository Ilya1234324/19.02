# Отчёт о работе с Git

Здесь описан мой процесс работы с Git и GitHub при выполнении задания 19.02.2026.

## 🚀 Этапы работы

1.  **Инициализация**
    *   `git init` — инициализировал Git в папке «Задания 19.02» (репозиторий уже существовал).

2.  **Подготовка файлов**
    *   `git add .` — подготовил все файлы в папке к сохранению (символ точки `.` означает «все файлы»).

3.  **Создание коммита**
    *   `git git commit` — ❌ **ошибка:** опечатка (двойной `git`).
    *   `git commit -m "Initial commit"` — ✅ успешно создал первый «снимок» (коммит) файлов с сообщением "Initial commit".

4.  **Настройка ветки**
    *   `git branch -M main` — переименовал основную ветку с `master` на `main` (современный стандарт).

5.  **Связь с GitHub (первые шаги)**
    *   `git remote add origin https://...` — ❌ **ошибка:** попытался привязать локальную папку к новому репозиторию на GitHub, но получил отказ, так как связь с другим репозиторием уже существовала.
    *   `git remote -v` — проверил существующие связи. Оказалось, что репозиторий привязан к старой папке (от 18.02.2026).

6.  **Перенастройка связи**
    *   `git remote set-url origin https://...` — ✅ изменил ссылку на **новый, правильный репозиторий** (для работ от 19.02).

7.  **Отправка на GitHub (Push)**
    *   `git push origin main` — ✅ отправил все файлы на GitHub в репозиторий `19.02`. Всё сжалось и «улетело»!

8.  **Дополнительная настройка (для продвинутых)**
    *   `git remote add new-origin https://...` — добавил ещё одну «связь» (ещё один удаленный репозиторий) с именем `new-origin`.
    *   `git push -u origin main` — ✅ закрепил связь между локальной веткой `main` и веткой на GitHub (`origin main`). Ключ `-u` (флаг `--set-upstream`) позволяет в будущем отправлять изменения простой командой `git push`.
